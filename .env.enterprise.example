# Azure no longer requires a separate environment file.
# Open Settings -> Model services -> Azure OpenAI and configure the key,
# service endpoint, deployment name and API version.
# Existing .env.enterprise files can be imported through Settings or:
# tradingagents config import-env --enterprise-file .env.enterprise
# Add --apply after reviewing the preview.
