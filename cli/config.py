CLI_CONFIG = {
    # Announcements. This fork does not ship an announcements service, so the URL
    # is blank: fetch_announcements() fails fast and falls back to the link below
    # (no phone-home to the upstream endpoint).
    "announcements_url": "",
    "announcements_timeout": 1.0,
    "announcements_fallback": "[cyan]For more information, please visit[/cyan] [link=https://github.com/qingchiang/trading-agents-x]https://github.com/qingchiang/trading-agents-x[/link]",
}
